const readFile = require('./readFile').readFile;

const runFunctions = (input) => {
    first(input);
    second(input);
}

const first = (input) => {   
    
};

const second = (input) => {
    
};

readFile('day9Input.txt', runFunctions);